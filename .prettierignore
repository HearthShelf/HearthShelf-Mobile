node_modules
android
ios
packages/core
*.md

# Docs are written and read by hand, not built. The design mockups in here are
# also hand-written HTML, several with unclosed tags that prettier refuses to
# parse - which would keep format:check permanently red.
docs
